{
  "version": 2,
  "buildCommand": "npx prisma generate && yarn build",
  "installCommand": "yarn install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dist/index.js"
    }
  ]
}

