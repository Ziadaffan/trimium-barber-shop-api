{
  "version": 2,
  "buildCommand": "npx prisma generate",
  "installCommand": "yarn install",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}

