{
  "version": 2,
  "buildCommand": "npx prisma generate && yarn build",
  "installCommand": "yarn install",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}

